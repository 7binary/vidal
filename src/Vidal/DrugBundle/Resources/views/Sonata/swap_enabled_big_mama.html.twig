<td class="swap">
	<a class="btn btn-swap {{ object.enabled ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_big_mama', {'field':'enabled', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>