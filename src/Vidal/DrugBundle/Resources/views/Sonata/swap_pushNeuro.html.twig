<td class="swap">
	<a class="btn btn-swap {{ object.pushNeuro ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_field', {'field':'pushNeuro', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>