<td class="swap">
	<a class="btn btn-swap {{ object.mobileProduct ? 'btn-positive' : 'btn-negative' }}"
	   href="{{ path('swap_main', {'field':'mobileProduct', 'entity':getClass(object), 'id':object.id}) }}">
	</a>
</td>